#include <stdio.h>
#include "main.h"

/**
* mul - a function that multiplies two numbers a and b
* @a: the first number to be multiplied
* @b: the second num to be multiplied
* Return: it's implicit return function
*/

int mul(int a, int b)
{
	return (a * b);
}
